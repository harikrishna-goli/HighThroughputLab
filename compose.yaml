services:
  postgres-primary:
    image: postgres:18-alpine
    container_name: postgres-primary
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      REPLICATION_PASSWORD: ${REPLICATION_PASSWORD:-replicapass456}
      PGDATA: /var/lib/postgresql/
    command:
      - postgres
      - -c
      - wal_level=replica
      - -c
      - max_wal_senders=16
      - -c
      - max_replication_slots=10
      - -c
      - hot_standby=on
      - -c
      - listen_addresses=*
      - -c
      - wal_keep_size=1GB
      - -c
      - max_connections=300
    ports:
      - "5432:5432"
    volumes:
      - postgres_primary_data:/var/lib/postgresql/
      - ./docker/postgres/init-replication:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-replica-1:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      PGDATA: /var/lib/postgresql/data
      PRIMARY_HOST: postgres-primary
      REPLICATION_PASSWORD: ${REPLICATION_PASSWORD:-replicapass456}
    volumes:
      - postgres_replica_1:/var/lib/postgresql/data
      - ./docker/postgres/replica-entrypoint.sh:/replica-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/replica-entrypoint.sh"]
    depends_on:
      postgres-primary:
        condition: service_healthy
      db-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 180s

  postgres-replica-2:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      PGDATA: /var/lib/postgresql/data
      PRIMARY_HOST: postgres-primary
      REPLICATION_PASSWORD: ${REPLICATION_PASSWORD:-replicapass456}
    volumes:
      - postgres_replica_2:/var/lib/postgresql/data
      - ./docker/postgres/replica-entrypoint.sh:/replica-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/replica-entrypoint.sh"]
    depends_on:
      postgres-primary:
        condition: service_healthy
      db-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 180s

  postgres-replica-3:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      PGDATA: /var/lib/postgresql/data
      PRIMARY_HOST: postgres-primary
      REPLICATION_PASSWORD: ${REPLICATION_PASSWORD:-replicapass456}
    volumes:
      - postgres_replica_3:/var/lib/postgresql/data
      - ./docker/postgres/replica-entrypoint.sh:/replica-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/replica-entrypoint.sh"]
    depends_on:
      postgres-primary:
        condition: service_healthy
      db-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 180s

  postgres-replica-4:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      PGDATA: /var/lib/postgresql/data
      PRIMARY_HOST: postgres-primary
      REPLICATION_PASSWORD: ${REPLICATION_PASSWORD:-replicapass456}
    volumes:
      - postgres_replica_4:/var/lib/postgresql/data
      - ./docker/postgres/replica-entrypoint.sh:/replica-entrypoint.sh:ro
    entrypoint: ["/bin/sh", "/replica-entrypoint.sh"]
    depends_on:
      postgres-primary:
        condition: service_healthy
      db-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 15
      start_period: 180s

  redis:
    image: redis:7-alpine
    container_name: financial-redis
    command:
      [
        "redis-server",
        "--maxmemory",
        "1gb",
        "--maxmemory-policy",
        "allkeys-lru",
        "--appendonly",
        "no",
      ]
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  db-init:
    build:
      context: ./FinancialApp-1MRps
      dockerfile: Dockerfile
    container_name: financial-db-init
    command: ["python", "init_db.py"]
    environment:
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_POOL_SIZE: 5
      DB_MAX_OVERFLOW: 5
      DB_POOL_TIMEOUT: 30
      WRITE_DB_HOST: postgres-primary
    depends_on:
      postgres-primary:
        condition: service_healthy

  haproxy-read:
    image: haproxy:3.0-alpine
    container_name: financial-haproxy-read
    volumes:
      - ./haproxy-read.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    expose:
      - "15432"
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres-replica-1:
        condition: service_healthy
      postgres-replica-2:
        condition: service_healthy
      postgres-replica-3:
        condition: service_healthy
      postgres-replica-4:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "haproxy", "-c", "-f", "/usr/local/etc/haproxy/haproxy.cfg"]
      interval: 10s
      timeout: 5s
      retries: 3

  api:
    build:
      context: ./FinancialApp-1MRps
      dockerfile: Dockerfile
    environment:
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_POOL_SIZE: 5
      DB_MAX_OVERFLOW: 10
      DB_POOL_TIMEOUT: 30
      UVICORN_TIMEOUT_KEEP_ALIVE: 10
      REDIS_URL: redis://redis:6379/0
      ACCOUNT_CACHE_TTL_SECONDS: 120
      READ_DB_HOST: haproxy-read
      READ_DB_PORT: 15432
      WRITE_DB_HOST: postgres-primary
    depends_on:
      db-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      haproxy-read:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'python -c "import urllib.request; urllib.request.urlopen(''http://localhost:8000/health'').read()"',
        ]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 15s
    expose:
      - "8000"

  nginx:
    image: nginx:1.27-alpine
    container_name: financial-nginx
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nginx -t || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

  locust:
    build:
      context: .
      dockerfile: Dockerfile.loadtest
    container_name: financial-loadtest
    environment:
      LOCUST_HOST: http://nginx
    depends_on:
      nginx:
        condition: service_healthy
    ports:
      - "8089:8089"
    volumes:
      - ./results:/loadtest/results
    profiles:
      - loadtest

volumes:
  postgres_primary_data:
  postgres_replica_1:
  postgres_replica_2:
  postgres_replica_3:
  postgres_replica_4:
